<!-- 模板 v1.2 | sync: auto | target: _workspace/_表单/_模板-编码任务卡.md -->
# T-<任务号> · <任务名>

- 状态：<进行中（附接手点一句话——新 AI 从这里继续）｜已完成（PASS+日期+commit）｜返工中｜阻塞（附原因与预案）>
- 执行模型：<provider/model（来源：交互选择｜显式参数｜环境变量:MODEL_ID）；一任务多模型接力逐段注明>
- 开工依赖核对：<待开工门禁回填；直接依赖任务/接口/数据契约→实际核对位置；无直接依赖写「无」>

## 交接检查（追加记录与归纳）

> 每次实际检查新增一条 `#### 检查 N`，不得修改、删除、合并或以新结论覆盖既有检查记录。勘误只能在新记录中说明原记录位置、勘误原因和更正事实。

### 检查记录

#### 检查 1
- 检查任务：<本任务号>
- 检查时间：<YYYY-MM-DD；未发生则写「待检查」>
- 触发方式：<后继任务开工门禁｜主动复检；主动复检注明发起者或原因>
- 检查依据：<本卡验证命令及输出、commit/工作区状态、实现文件清单、对照表行、待复核项的实际核对位置>
- 语义核对：<驱动依据或验收条款→代码位置→验证证据；覆盖的边界/负向场景；发现或「无」>
- 检查模型：<provider/model（来源：…）；未发生则写「未记录」>
- 模型独立性：<独立：上一任务执行模型=…；或同模型：原因=…；无法比较：上一任务精确型号未知>
- 六项结果：<逐项 PASS/FAIL 与摘要；发现问题记「发现→定因→处置」并同步观察日志。>

### 归纳总结
- 检查记录数：<N；无检查写 0>。
- 稳定共识：<各记录均确认的事实；无则写「无」>。
- 差异与处置：<不同结论、覆盖范围或环境结果→定因→处置；无差异写「无」>。
- 未决项：<仍待复核的代码/数据/环境事项及责任落点；无则写「无」>。

## 做什么
<一两句话+验收口径（这个任务做完时什么成立）>

## 驱动依据
<设计文档/图表+节号；数据文件给绝对路径或登记表指向——写到「新 AI 不用问人就能定位」的程度>

## 实现记录
<文件清单+关键实现决定（编码期自由度内的选择）>

## 遇到的问题
<逐条：现象→原因→处置。已解=写解法；未解=写预案。
 没有问题写「无」。环境与数据的坑（编码/路径/格式意外）必须记——不记，下个会话原样再踩。>

## 验证
<命令/用例号+实际输出摘要+结果（PASS/FAIL）+验证环境（运行时版本、数据路径）>

## 偏差与决定
<与驱动依据不符处：实现偏差（改码）或设计缺口（走变更登记）。
 没有写「无」。不得留空——这是审核的主要抽查对象。>

## 追溯
<commit 号 ←→ 实现对照表行 ←→ 用例号（如有）>
