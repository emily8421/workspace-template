<!-- 模板 v1.0 | sync: manual | target: <实例各目录>/AGENTS.md -->
# AI Agent 入口（<目录名>）

进入本目录后，先读取并遵守：

1. `<工作区根>\_workspace\00-总入口.md`（全局规则）
2. 本目录 `README.md` 与自有规则（如有 `00-AI规则\` 等，按编号阅读）

新开会话先查 `<工作区根>\_workspace\_handoff\README.md`。
续接文档写入 `<工作区根>\_workspace\_handoff\<目录名>\`。