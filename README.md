<!-- 模板 v1.2 | sync: manual | target: <实例根>/README.md -->
# <工作区名> · 工作区总览

<一句话：这个工作区装了什么、在推进什么。>

本文件来自工作区模板；模板的用法和新手教程见模板仓的 `模板说明.md` 与 `使用手册.md`。AI 入口见 `AGENTS.md`（Codex）和 `CLAUDE.md`（Claude Code），两者都指向 `_workspace/00-总入口.md`。

## 从哪里开始

| 你的情况 | 先看 |
|---|---|
| 想了解本工作区的结构 | 本文件，再 `_workspace/01-目录索引.md` |
| 新会话的 AI（Codex / Claude Code） | `AGENTS.md` / `CLAUDE.md` → `_workspace/00-总入口.md` |
| 接手进行中的任务 | `_workspace/_handoff/README.md`（顶部「当前主线 / 下一任务」） |
| 刚建好工作区、要做首次配置 | `_workspace/05-落地检查清单.md`（按阶段 1-4 逐项打勾） |
| 想再建一个工作区 / 不熟悉本套模板 | 模板仓 `模板说明.md`（快速概览）与 `使用手册.md`（新手教程） |

## 目录地图

| 目录 | 用途 | 是否默认创建 |
|---|---|---|
| `_workspace/` | 全局规则、目录索引、续接记录、表单和示例 | 是 |
| `01-文档/` | 需求、方案、会议记录、阶段产物等正式文档 | 是 |
| `04-工具/` | 可复用脚本、维护工具和使用说明 | 是 |
| `02-数据/` | 数据源登记、索引和访问说明；原始数据通常不入仓 | 否 |
| `03-知识输入/` | 只读参考资料及其索引 | 否 |

详细的绝对路径登记见 `_workspace/01-目录索引.md`。

## 日常工作速记

- 文档写 `01-文档/`，脚本写 `04-工具/`；原始数据和参考资料按只读规则登记，不直接搬入。
- 新会话（人或 AI）先看 `_workspace/_handoff/README.md` 的「当前主线 / 下一任务」。
- 新增、移动或重命名目录后，同步更新 `_workspace/01-目录索引.md` 和相关引用。
- 改 VS Code 挂载只改 `_workspace/07-VSCode工作区挂载登记.md` 的 json 块，再运行 `04-工具\sync-vscode-workspace.ps1 -DryRun` 预览，确认后去掉 `-DryRun` 执行。

## 硬约束

- 凭据、token、私钥不得写入文档或提交到 Git。
- 被登记为只读的目录不得修改；跨目录写入默认写回所属目录。
- 未经确认不得执行 `git push`，也不得删除或归档目录。

完整规则以 `_workspace/00-总入口.md` 和 `_workspace/02-跨目录约束.md` 为准。

## 当前主线

<本阶段在推进什么；无进行中任务时写「无」，并指向 `_workspace/_handoff/README.md`。>
