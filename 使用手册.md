# 使用手册（新手教程）

> 面向第一次接触本模板的人。不需要先读懂规则文件，从头跟着走一遍，就能建出第一个工作区并开始日常使用。
> 只要快速概览的，读 `模板说明.md` 的「一分钟看懂」；规则正文在各实例的 `_workspace/` 下。
> 本文件是模板仓自身文件，不复制进实例。模板维护脚本在 `_模板维护\`，历史提案归档在 `_模板维护\提案\`。

## 1. 这套模板解决什么问题

一个人用多个目录放文档、脚本、数据，再让 AI（Codex、Claude Code 等）帮忙干活时，常遇到三件事：

1. 文档散放，找不到权威版本，新旧两份并存；
2. AI 每次新开会话都不知道上次做到哪；
3. 换个目录启动 AI，它读不到之前定好的规则，各处行为不一致。

本模板用一套固定骨架解决这三件事：

```text
模板仓 ── create-instance.ps1 ──> 新工作区（实例）

实例内的固定骨架：
  _workspace/      治理区：规则、目录索引、任务续接记录（AI 开工前先读）
  01-文档/         人读的正式文档
  04-工具/         可执行脚本
  02-数据/         可选区：数据源登记（默认不建）
  03-知识输入/     可选区：只读参考（默认不建）
```

三件事的对应解法：问题 1 靠治理区的登记表和「唯一事实源」规则（`_workspace/08-通用文档管理原则.md` A1）；问题 2 靠 `_workspace/_handoff/` 续接记录；问题 3 靠每个目录放 `AGENTS.md` / `CLAUDE.md` 入口，把 AI 引到 `_workspace/00-总入口.md`。

模板只管「怎么管理文档和 AI 会话」，不管「你放什么业务内容」——目录里装什么由每个实例自己定。

## 2. 概念速查

读其他文档遇到不懂的词，回这张表查。

| 术语 | 白话解释 |
|---|---|
| 模板仓 | 本目录，模板本身所在的地方，所有实例的母本。 |
| 实例 | 用模板复制出来的一份真实工作区，可以建多个，互不影响。 |
| 治理区（`_workspace/`） | 实例里管规则的地方：全局规则（00-09 号文件）、目录索引、续接记录、空表单。 |
| 落地 | 把模板变成可用实例的过程：填占位符、登记目录、建 git 仓、首次提交（见第 5 节）。 |
| 续接 / `_handoff/` | 任务中断时留下的记录；下次新会话读了就能接着干，不用重新交代背景。 |
| 挂载 / `.code-workspace` | 把目录列进 VS Code 的多根工作区文件（一个窗口同时打开多个目录）。挂载只影响 VS Code 显示，不影响 AI 读规则。 |
| `AGENTS.md` / `CLAUDE.md` | AI 的入口文件。Codex 读 `AGENTS.md`；Claude Code 读 `CLAUDE.md`，不读前者。两者都指向 `_workspace/00-总入口.md`。 |
| 唯一事实源 | 一个主题只认一个锚点文件，其他材料用链接指向它，不复制内容（A1 原则）。 |
| sync: auto / manual | 写在模板文件头注释里的同步属性。auto = 模板更新可安全覆盖实例文件；manual = 实例文件含自己的内容，模板更新时需人工合并。 |
| 拓扑 A / B | git 布局两种选法。A = 治理区、文档、工具各自独立 git 仓；B = 整个工作区一个仓。 |
| 可选区 | `02-数据/` 和 `03-知识输入/` 两个目录，默认不创建，加 `-IncludeOptionalZones` 才建。 |

## 3. 准备

- Windows：实例创建、模板同步、挂载同步三个脚本都是 PowerShell 脚本。
- VS Code（推荐）：用来打开 `.code-workspace` 工作区。
- git：实例落地时要建仓提交。
- 模板仓一份：整目录复制，或 `git clone`。

## 4. 创建第一个工作区

三种方式选一种，效果相同：在指定父目录下生成一个新实例。

### 4.1 方式一：一键脚本（推荐）

在模板仓根目录运行，把 `<实例名>` 和 `<父目录>` 换成实际值（反引号是 PowerShell 的续行符，整段一起复制）：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_模板维护\create-instance.ps1 `
  -Name <实例名> `
  -Parent <父目录>
```

默认只建治理区、文档区、工具区。要同时建两个可选区，在末尾加 `-IncludeOptionalZones`：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_模板维护\create-instance.ps1 `
  -Name <实例名> `
  -Parent <父目录> `
  -IncludeOptionalZones
```

成功时最后输出「实例已创建: <路径>」和下一步提示。生成内容的核对清单见第 8 节 FAQ 第 3 条。

### 4.2 方式二：手动复制

1. 复制整个模板仓目录，改名成 `<实例名>/`；
2. 删除模板自身文件：`模板说明.md`、`使用手册.md`、`CHANGELOG.md`、`_模板维护/`（两个脚本和提案归档都在里面）、根 `.gitignore`、`.git`；
3. 把 `模板-工作区.code-workspace` 改名为 `<实例名>.code-workspace`；
4. 按第 5 节初始化。

### 4.3 方式三：让 AI 代办

在目标父目录（或实例目录）启动 AI，直接说人话，它会读模板说明后执行方式一。常用说法：

| 场景 | 可以直接对 AI 说 |
|---|---|
| 创建工作区 | 「基于 `<模板仓路径>` 创建工作区 `<实例名>`，放在 `<父目录>`；不创建数据区和知识输入区。」 |
| 创建含可选区的工作区 | 「基于模板创建 `<实例名>`，同时创建 `02-数据` 和 `03-知识输入`。」 |
| 开始或续接任务 | 「处理 `<任务>`。先读取当前工作区规则和 `_workspace/_handoff/README.md`，再开始。」 |
| 调整目录或迁移内容 | 「把 `<内容>` 迁移到 `<目标目录>`；更新登记和引用，保留原始资料。」 |

AI 应先读规则、再说明要改哪些文件和为什么；涉及只读目录、删除、提交、推送时会向你确认。

## 5. 初始化（创建后的第一次配置）

逐项打勾版在实例的 `_workspace/05-落地检查清单.md`，这里是走读版。四个阶段按顺序走。

### 阶段 1：填占位符、做登记

1. 实例根 `README.md`：`<工作区名>` 换成实际名字，「当前主线」按实际填（没有任务就写「无」）。
2. 实例根 `CLAUDE.md`：`@` 后面的路径替换成实例的 `_workspace/00-总入口.md` 真实绝对路径（用正斜杠）。不替换，Claude Code 读不到全局规则。
3. `_workspace/01-目录索引.md`：目录行换成实际绝对路径（盘符开头），补用途。这张表是「AI 从任何目录启动都能看懂全局」的依据。
4. `_workspace/02-跨目录约束.md`：登记哪些目录只读、凭据相关边界、例外可写项。
5. 核对 `_workspace/07-VSCode工作区挂载登记.md` 的 json 块，跑一次挂载同步预览（见 6.4），新实例应显示无变更。

### 阶段 2：迁移旧资料（有旧资料才做）

- 旧资料按「文档进 `01-文档/`、脚本进 `04-工具/`」搬入，旧目录保持只读不动；
- 搬完跑断链检查：在实例根执行 `rg -n "旧目录名|旧文件名" .`，确认没有残留旧路径引用（没装 rg 就用 VS Code 全局搜索）。

### 阶段 3：建 git 仓、首次提交

1. 选拓扑：目录多、有大数据或已有多个旧仓，选 A（各区独立仓）；全新小工作区、纯文档，选 B（单仓）。判据见 `_workspace/03-git管理.md`。
2. 逐仓 `git init`，配好 `.gitignore`（凭据、大文件、编辑器状态）。
3. 提交前检查暂存区：没有凭据文件、大文件、嵌套 `.git`。
4. 首次提交，信息格式「类型(范围): 中文描述」，如 `docs(治理): 初始化工作区`。

### 阶段 4：收尾

- 把初始化结果记进 `_workspace/_handoff/README.md`（建一份续接文档并登记索引）；
- 确认不再用的旧目录，从挂载登记移除、归档或删除（删除前必须确认）。

## 6. 日常工作

### 6.1 东西放哪

| 要放的东西 | 放哪 | 补充 |
|---|---|---|
| 需求、方案、记录等正式文档 | `01-文档/` | 按项目建子目录，被取代的旧版移进 `归档/` |
| 可复用脚本 | `04-工具/` | 配使用说明；满足登记判据的登记进 `tool-registry.json`（规则见 `_workspace/09-工具管理.md`） |
| 原始数据 | 不搬进工作区，在 `02-数据/` 登记位置、约束和状态 | 可选区，需先启用 |
| 外部参考资料 | 小文件复制到 `03-知识输入/`（只读），大文件只登记不复制 | 可选区，需先启用 |

### 6.2 让 AI 干活的标准循环

1. 在实例目录（或其子目录）启动 AI；
2. 说清任务，或让它先读 `_workspace/_handoff/README.md` 续接已有任务；
3. AI 应先读规则再动手；没读就提醒一句「先读 `_workspace/00-总入口.md`」；
4. 任务没做完要中断时，让 AI 把进展写成续接文档（见 6.3）。

### 6.3 任务中断与续接

- 中断前：让 AI 复制 `_workspace/_handoff/_模板-续接文档.md`，填成一份续接文档（一句话现状、待办、新会话怎么接），存进 `_handoff/<目录名>/`，并在 `_handoff/README.md` 登记一行、更新顶部「当前主线 / 下一任务」。
- 下次开工：新会话先读 `_handoff/README.md`，按指针找到续接文档，按其中「新会话怎么接」操作。

### 6.4 改 VS Code 挂载

1. 只改 `_workspace/07-VSCode工作区挂载登记.md` 的 json 块：加一行、删一行，或把某项的 `mount` 改 true/false；
2. 在实例根预览：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\04-工具\sync-vscode-workspace.ps1 -DryRun`
3. 确认无误后去掉 `-DryRun` 执行；VS Code 提示「工作区已更改」属正常，点重新加载。

不要手改 `.code-workspace` 的 `folders` 段——下次同步会被覆盖；`settings` 等其他字段可以自己改。

## 7. 接收模板更新

模板修了规则、你想让已建实例跟上时，在模板仓根目录先看差异（不加参数不会改任何文件）：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_模板维护\sync-template.ps1 `
  -Instance <实例目录>
```

输出分两组：auto 组是可安全覆盖的文件，加 `-Apply` 执行更新；manual 组（README、目录索引、跨目录约束这类含实例自己内容的文件）只能人工比对着合并。

## 8. 常见问题（FAQ）

**1. 运行 create-instance.ps1 报「目标已存在」**
`<父目录>` 下已有同名实例目录。换一个实例名，或确认旧目录可删后删掉重跑。删除前确认它是建错的实例，不是模板仓或其他在用的工作区。

**2. 报「禁止运行脚本」**
PowerShell 执行策略拦住了。用本手册的完整命令（带 `-NoProfile -ExecutionPolicy Bypass -File`），不要直接 `& .\_模板维护\create-instance.ps1`。

**3. 怎么确认创建成功**
`<父目录>\<实例名>\` 下有 `_workspace/`、`01-文档/`、`04-工具/` 和 `<实例名>.code-workspace`；`_workspace/` 里有 `00-总入口.md` 等编号文件和 `_handoff/`。

**4. 建完发现没有 02-数据 / 03-知识输入**
默认不建。后续想要：手动建这两个目录（各配 README + AGENTS，可照其他目录的写法），把挂载登记 json 块里对应项的 `mount` 改成 `true`，跑一次同步脚本；或者删掉实例加 `-IncludeOptionalZones` 重建。

**5. `.code-workspace` 是什么**
VS Code 多根工作区文件，双击或用 VS Code「文件 → 打开工作区」打开，多个目录列在同一个侧边栏。它只是 VS Code 的打开方式，AI 读规则不靠它。

**6. AI 开工不读规则**
依次检查：启动目录是否在实例内；实例根和各目录的 `AGENTS.md` / `CLAUDE.md` 是否存在、`CLAUDE.md` 的 `@` 路径是否已换成实例真实路径；还不行就在提示词里直接写「先读 `_workspace/00-总入口.md` 再开始」。

**7. sync-template.ps1 列出一片 manual 差异**
正常。manual 文件在实例里有自己的内容，模板不自动覆盖。逐项人工合并；与自己实例无关的差异可以不处理。

**8. 能在 Mac / Linux 用吗**
三个脚本（创建实例、同步模板、同步挂载）是 PowerShell，面向 Windows。手动复制骨架的方式跨平台可用，三个脚本的功能需自行替代；规则文档本身不依赖平台。

**9. 文件里的 `<工作区名>`、`<实例名>` 这类尖括号**
占位符，实例化时必须替换成实际值（初始化阶段 1 的第一项）。一直不替换，人和 AI 会一直看到占位符。
