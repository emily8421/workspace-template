<!-- 模板 v1.0 | sync: manual | target: <实例>/02-数据/AGENTS.md -->
# AI Agent 入口（02-数据）

进入本目录后，先读取并遵守：

1. `<工作区根>\_workspace\00-总入口.md`（全局规则）
2. 本目录 README.md（数据源登记表）与 `索引\`（大型数据源索引清单）

## 硬约束

- 本目录为**数据登记区**：只登记、不存本体；本体在旧目录/外部盘，仅按需点读/检索，禁止写入数据源本体。
- 凭据禁入：`.gitlab-token` 等凭据文件禁读内容、禁提交。
- 续接文档写入 `<工作区根>\_workspace\_handoff\02-数据\`。