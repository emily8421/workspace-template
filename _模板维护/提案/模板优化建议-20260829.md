# workspace-template 优化建议（2026-08-29 工程规范层回流提案）

> **状态：已落地（v1.2 批二，2026-08-29 用户裁定通过）**——前提决策采纳「启用 00-规范 规范层」；6 项按瘦身方案落地，与同日另一批（README 瘦身+使用手册）合并登记 CHANGELOG v1.2。本文件保留为决策记录。
> **来源**：实例 `miiot_plugin_develop_system` 治理规则层与模板同步核对（2026-08-29，dry-run 确认 auto 6 文件零差异）之后，对实例 `01-文档/00-规范/` 规范建设的回流扫描。覆盖期间为 2026-08-27 至 2026-08-29（实例规范层集中建设期）。
> **核心原则**（沿用 20260808/20260815/20260825 版）：模板 = 通用文档管理骨架；业务目录 = 实例自由区。回流提炼「机制」，不回流实例路径与业务术语。

---

## 〇、本提案的前提决策：规范层落点

以往三轮回流的落点只有两类：治理区规则（`_workspace/` 00-09 号）与 `_表单/`、`_参考示例/`。本次候选主体是工程规范（怎么画图、怎么评估文档、怎么记编码任务），两类落点都不合适：规范不是填空的表单，也不宜降级为「示例，非规则」。

模板 `01-文档/README.md` 规划子结构中已有 `00-规范/`（「编码规范等，按需创建」）——规划位存在但从未启用。本提案建议启用该层，并约定三条：

1. 模板 `00-规范/` 文件**不带数字编号**（实例编号体系由实例自定；实例现有 00-规范 已编至 11 号，模板带号会在实例化时制造撞号）。
2. front-matter 标 `sync: manual`——规范实例化后归实例维护，模板升版只提示不覆盖。
3. `create-instance.ps1` 为整树复制，新目录自动随骨架复制，无需改脚本。

若维护者裁定不启用规范层，本提案 #1/#2/#3/#5 全部降级为 `_表单/_模板-*.md`（复制后启用），机制内容不变，仅落点变。

## 一、回流建议总表

| # | 机制 | 实例出处 | 通用性 | 建议 | 优先级 |
|---|---|---|---|---|---|
| 1 | 图表建模规范（图型选型+通用七条+分图型细则+工具语法坑） | `00-规范/11-图表建模规范.md` v0.4 | 最高（零业务，坑均本工作区实测） | **进模板 00-规范（瘦身版）** | 高 |
| 2 | 文档评估规范（11 维度+类型侧重+五步流程+处置分级+机扫机制） | `00-规范/09-文档评估规范.md` v0.2 | 高（三轮评估实践提炼） | **进模板 00-规范（瘦身版）** | 高 |
| 3 | 编码任务卡机制（漏评估补账：建于 08-20，未入 0815/0825 两轮扫描窗口） | `00-规范/05-编码实现与变更控制规范.md` v1.0 | 高（AI 编码接力=模板核心场景） | **进模板 00-规范（瘦身版）+ 卡模板进 _表单** | 高 |
| 4 | 指向行同步条款（指向正本的行随正本升版刷新） | 设计图表 README 维护规则（实例 08-28 `0ba27f6`） | 高（单条，零成本） | **并入模板 08 号 A3** | 中 |
| 5 | 范围号全角化（半角 `~` 被渲染为下标） | 实例 09 号机扫 pattern（08-27 `2230ce5`，43 文件 420 处） | 高（单条，markdown 渲染通坑） | **随 #2 进机扫清单；#2 不落地则入 08 号注记** | 中 |
| 6 | 数据资产管理纪律四条（来源四档分级/批次纪律/只读与问题闭环/预留槽位） | `00-规范/10-数据资产管理方法.md` v0.1 | 中（多数据加工实例适用；纯文档实例用不到） | **进模板 00-规范（仅抽四节，约 40 行）** | 中 |

## 二、逐项说明

### 1. 图表建模规范（价值最高）

**问题**：模板对「开发过程文档中的模型图怎么画」完全空白。AI 画图无约束时常见：一张图混多个主题、元素无稳定编号、图内术语与正文漂移、工具语法踩坑（mermaid/plantuml 渲染失败后反复试错）。

**回流内容**（实例 v0.4 全 100 行，通用版约 70 行）：

- **§0 图型与工具默认选择**：五图型选型表（用例图=plantuml 唯一、类图/顺序图/状态图/结构图=mermaid 预览优先）+ 三总则（目标阅读环境可渲染优先 / 同图型同文档不混工具 / 迁工具须在版本记录登记）。
- **§1 通用七条** G1～G7：一图一主题 / 7±2 上限与拆分维度优先级 / 元素唯一编号（拆图不改编号只改分组）/ 图题三要素（图名+版本+日期，随主文档登记不单独立版本记录）/ 图内术语与正文同名 / 非标准记法必写图例 / 正本（源码块）与副本（渲染产物）分离。
- **§2～6 分图型细则**：用例图（参与者边界、系统内参与者记法、include/extend 用法、连线密度信号）、分析类图（粒度、实体须有锚点、超限优先 package 分组呈现不拆页、新概念回填义务）、设计类图（分层细化不改职责边界、追溯）、顺序图（只画主链用例、超 15 条消息按阶段分屏、异常用 alt 片段）、状态图/ERD/事物—事件表最小规则。
- **附录 A 工具语法约束六条**（全为实例实测坑）：mermaid 禁 PlantUML 多重度记法、plantuml 两行标签写字面 `\n` 序列且批量替换须按两字面字符匹配（附误删事故教训）、mermaid 注释 `%%`、围栏标注约定、classDiagram 不支持 `package` 须用 `namespace`、关系线标签内禁半角冒号与半角括号。

**瘦身**：删除实例现状注记（如「本项目 17 用例/6 actor 一张全图不追溯」「三库分层、品类字典」示例、「SRS 附件 N 先例」锚点）；「02/03/04 号规范」「07 号台账」等跨文档引用泛化为「项目阶段规范」「版本记录」；依据列的实例提交号（DG-04、附件 1 事故等）保留为「实例实测」简注，不携带路径。

**通用性**：零业务内容。附录 A 的坑对任何 markdown+mermaid/plantuml 工作区成立，且均经实例 2026-08-18/08-28 两轮渲染事故验证——这类坑不回流，每个新实例重踩一遍。

### 2. 文档评估规范

**问题**：用户说「评估 XX 文档」时，AI 无执行依据则维度随心情、结论无分级、修订处置无判据。实例三轮评估实践（URS v1.11/SRS v3.4/G3 概设）后固化为规范。

**回流内容**（实例 v0.2 全 76 行，通用版约 60 行）：

- **11 维度** D1～D11：内容完整性 / 逻辑正确性 / 无歧义性 / 跨文档一致性 / 可追溯性 / 可验证性 / 必要性（防镀金）/ 分级清晰度 / 语言规范性 / 状态时效性（批准基线内不得残留评审期状态词）/ 引用完整性。每维度带「查什么+主要方法」。
- **类型侧重表**：需求类全维度；设计类重 D2/D4/D5/D11；计划台账类重 D4/D10/D11；评审裁定材料重 D2/D4/D10；规范类重 D4/D10。
- **五步流程**：定事实源（识别上游基线与最近 diff）→ 机扫 → 人工复核 → 出差异清单（必修/建议修/可选/留痕不动四级，每条含定位+问题+依据+改法）→ 裁定与执行。
- **处置分级**：表述级勘误（不升版）/ 一致性补齐（指针化）/ 内容变更（走变更登记后升版）——三档判据。
- **机扫 pattern 机制**：表格形态（目的/pattern/备注），「随实践追加」。

**瘦身**：D1 的国标索引引用改可选注记（模板无该索引）；机扫 pattern 中实例术语基线条目（「客户」排除「需方」）标注为实例示例，通用版只保留 pattern 形态与三条通用条目（A9 高频词/弱强度词/编号重复）；「07 号台账」「15 号 §4」引用泛化。

### 3. 编码任务卡机制（漏评估补账）

**问题**：本机制建于 2026-08-20，恰在 20260815 回流落地（08-17）与 20260825 单点增补之间，两轮扫描均未覆盖 `00-规范/` 目录，漏评估至今。其核心定位「任务记录的第一读者是下一个 AI 会话」与模板核心场景（AI 接力）完全重合，是本轮最应补账的一项。

**回流内容**（实例 v1.0 全 282 行，通用版约 200 行）：

- **任务拆分**：子系统开工前拆卡；一任务=一个可独立验证的构件（独立验证锚点判据）；不预拆未开工子系统。
- **任务卡十节结构**：状态（附接手点一句话）/ 执行模型（provider/model+来源，禁止产品族名伪装型号）/ 交接检查（append-only 多记录+归纳）/ 做什么 / 驱动依据（设计文档到节号，不到节号=未定位）/ 实现记录 / 遇到的问题（必填，环境与数据的坑不记则下个会话原样再踩）/ 验证（命令+输出摘要，未实跑不得记 PASS）/ 偏差与决定（实现偏差 vs 设计缺口二分，不得留空）/ 追溯。
- **三层交接开工检查**：交付证据六项机械核对（commit 真实存在/验证命令复跑一致/文件清单吻合/必填节非空/对照表已登记/待复核有下文）+ 语义核对（依据条款→代码位置→验证证据三段式，测试已过但未覆盖条款记为覆盖缺口）+ 直接依赖核对（检查对象可不同于时间上一任务，防并行遗漏真实前置）。
- **模型独立性**：检查模型优先不同于执行模型；同模型须记原因；精确型号未知如实标注，不得伪造。
- **验证环境=运行时环境**：嵌入运行时（Electron/内嵌 Node/浏览器）能力必须在该运行时实际验证，宿主环境不得代验（实例案例：系统 Node 通过但 Electron 装配失败）。
- **结构健康检查**：公共入口只导出装配不加业务实现、依赖方向不反转、行数阈值只触发复核不自动判问题。
- **追溯补卡**：建卡机制前的已完成工作按现存提交记录还原补卡，验证命令当前可复跑才记 PASS。
- **新会话接力链**：续接索引→阶段续接文档→对照表→上一张已完成卡→进行中卡→驱动依据节号，每层只指下一层。
- **变更控制**：设计缺口不得在代码里自行发明设计，停任务走变更机制。

**瘦身**：删 §10 实例映射表（FlowKit 专属）；「本项目=15 号 §4 对照表」泛化为「项目实现对照表」；`start-flowkit-task.ps1` 门禁脚本不随模板复制（实例按需自建，判例同 v1.1 工具管理 validate/render 脚本省略）；观察日志 v2 字段细则压缩为机制段。

**配套表单**：`_表单/_模板-编码任务卡.md`——十节空卡结构，与规范同步回流。

### 4. 指向行同步条款（单条并入 08 号）

实例 08-28 裁定：「指向正本的行（README 中的版本指向、状态行）须随正本升版刷新；不刷即登记缺口」。实例实测曾出现指向行滞后正本四版。本条是 A3（引用完整性）的自然延伸：A3 管「移动/改名后不断链」，本条管「正本升版后指向行不滞后」。**回流方式**：模板 08 号 A3 补一条，约两行。

### 5. 范围号全角化（单条）

半角 `~`（U+007E）作范围号时，部分 markdown 渲染器将成对 `~` 解析为下标，范围尾部数字变下标（实例 2026-08-27 SRS 44 处、全仓 43 文件约 420 处修复）。范围号一律用全角 `～`。**回流方式**：随 #2 进机扫 pattern 清单（实例现行做法）；#2 不落地则在 08 号 A7 自检清单补一行。

### 6. 数据资产管理纪律（部分回流）

实例 10 号 v0.1 共 120 行，主体（model_key 主轴、三库容器、五类配套、存储衔接）为业务内容不回流。抽出四个通用子机制，约 40 行：

- **资产来源四档分级**：直接装载 / 抽取加工 / 分析沉淀 / 运行期自产，各档获取方式与启动瓶颈不同——不得把运行期自产当作可从原始数据抽取的内容来规划生产。
- **批次纪律**：对外发布形成新批次不覆盖旧批次；分析结论必须标注所用批次与规则版本；**历史处理产物只作对照，不得作为正式基准**。
- **质量与问题纪律**：原始材料只读；未过准入的数据必须形成问题记录（依据+建议动作+责任角色），禁止静默丢弃；人工裁决写专门记录表，不改原始文件。
- **预留槽位模式**：为将来可能新增的证据类型预留占位字段（来源类型/存在状态/证据引用），缺位以默认值占位且查询可见——缺位不是缺陷，材料到达后填槽生效不改结构。

**通用性**：中。有数据加工环节的实例适用；纯文档实例用不到，故放 00-规范 按需阅读，不进治理规则。

## 三、不回流项（登记否决理由）

| 项 | 理由 |
|---|---|
| 10 号主体（model_key 主轴/三库容器/五类配套/存储两阶段衔接） | 业务数据模型，实例自由区 |
| 11 号的实例现状注记（「17 用例一张全图不追溯」等） | 实例裁定记录，非通用规则 |
| 09 号机扫的实例术语基线条目 | 实例术语约定（需方/客户） |
| 05 号 §10 FlowKit 映射、观察日志 v2 字段全表、start-flowkit-task.ps1 | 实例配套，机制已由正文承载 |
| 00-规范 01～08 号（SDLC 放行/需求/概设/详设/界面/SDK 约束等） | 项目 SDLC 过程规范，与本项目阶段门禁（G1～G5）绑定，属实例自由区；模板不携带过程规范 |
| 07 号台账/10 号控制记录/17 号 G4 实施计划等项目管理文档 | 实例产物 |
| G4 门禁与一册一循环纪律 | 同上，详设阶段过程规则 |

## 四、待办（给模板维护者）

1. [x] **先拍前提决策**：启用 `01-文档/00-规范/` 规范层（2026-08-29 用户裁定通过；三条约定见「〇」）
2. [x] #1 图表建模规范瘦身版落 00-规范；附录 A 六坑逐条保留
3. [x] #2 文档评估规范瘦身版落 00-规范；#5 全角～随入机扫清单
4. [x] #3 编码规范瘦身版落 00-规范 + `_表单/_模板-编码任务卡.md`；`_表单/README.md` 登记新表单行
5. [x] #4 指向行同步条款并入模板 08 号 A3（sync: auto，升 v1.2）
6. [x] #6 数据资产四节瘦身版落 00-规范（按需定位）
7. [x] `01-文档/README.md` 规划子结构行更新（00-规范 从「按需创建」改「模板预置通用规范+实例自建并列」）
8. [x] 落地后：升版本号 + 登记 CHANGELOG + commit；本文件标注「已落地」（实例侧 dry-run 待实例维护者执行）

## 附：本提案依据的实例产物

| 产物 | 位置 |
|---|---|
| 图表建模规范 v0.4 | `miiot_plugin_develop_system/01-文档/00-规范/11-图表建模规范.md`（`70939c5`～`0ba27f6`，2026-08-28） |
| 文档评估规范 v0.2 | 同目录 `09-文档评估规范.md`（`0d14ca7`，2026-08-27） |
| 编码实现与变更控制规范 v1.0 | 同目录 `05-编码实现与变更控制规范.md`（2026-08-20 建卡机制，v0.1～v1.0 十一轮迭代） |
| 数据资产管理方法 v0.1 | 同目录 `10-数据资产管理方法.md`（`637d375`～`bef417d`，2026-08-27） |
| 指向行同步条款 | 设计图表 README 维护规则（`0ba27f6`，2026-08-28） |
| 全角～修复 | SRS 44 处 `82ef63d` + 活跃层 43 文件 `2230ce5`（2026-08-27） |
| 治理规则同步核对 | 2026-08-29 `sync-template.ps1` dry-run：auto 6 文件零差异（A7 两条增补已于 08-27 双仓同源改 `629c96a`/`b7792b5`） |
