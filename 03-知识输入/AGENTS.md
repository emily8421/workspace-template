<!-- 模板 v1.1 | sync: manual | target: <实例>/03-知识输入/AGENTS.md -->
# AI Agent 入口（03-知识输入）

本目录为**只读参考知识库**。进入后先读取并遵守：

1. `<工作区治理区绝对路径>\_workspace\00-总入口.md`（全局规则）
2. 本目录 README.md（存放规则与约束）

## 硬约束

- 本目录**只读**：不修改、不新增、不删除任何内容；参考依据仅供查阅。
- 大型知识数据只读引用，索引见 `02-数据/README.md`。
- 续接文档写入 `_workspace/_handoff/03-知识输入/`。